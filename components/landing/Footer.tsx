export function Footer() {
  return (
    <footer className="landing-footer">
      <p>
        © {new Date().getFullYear()} ChronoSync — AI‑powered collaborative scheduling
        for teams, couples & academia
      </p>
    </footer>
  );
}
