import type { ReactNode } from "react";

/** Auth uses root Geist + globals; no marketing Inter/font-awesome. */
export default function LoginLayout({ children }: { children: ReactNode }) {
  return children;
}
